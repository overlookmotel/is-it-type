/* --------------------
 * is-it-type module
 * Tests
 * ------------------*/

'use strict';

// Modules
const isIt = require('../index');

// Tests

describe('tests', () => {
	it.skip('all', () => { // eslint-disable-line jest/no-disabled-tests
		expect(isIt).not.toBeUndefined();
	});
});
